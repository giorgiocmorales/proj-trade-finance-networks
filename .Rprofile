# Project startup configuration

# Mark the project root for the {here} package
if (requireNamespace("here", quietly = TRUE)) {
  try(here::i_am("README.md"), silent = TRUE)
}

# Default R options
options(
  repos = c(CRAN = "https://cloud.r-project.org"),  # stable CRAN mirror
  readr.show_col_types = FALSE                      # cleaner readr output
)
